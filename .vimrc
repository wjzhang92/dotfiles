colorscheme desert "specify color of background being used

set showcmd "show what you are typing as a command

set autoindent "adds in auto indents

set shiftwidth=4 "4 space auto indent

set tabstop=4 "4 space tab

set number "line numbers

set cul "highlight current line

set backspace=indent,eol,start "allow backspacing over autoindent, line breaks and start of insert action

set ruler "display cursor position on last line of screen

set mouse=a "enable use of mouse for all modes

set nostartofline "stop movements from always going to first character of line

set ls=2 "always show status line

set title "show title in console title bar

